spring:
  cloud:
    nacos:
      config:
        server-addr: 47.104.25.22:8888
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        file-extension: yml
        encode: UTF-8
      discovery:
        server-addr: 47.104.25.22:8888
        service: ${spring.application.name}
        register-enabled: true
swagger:
  enable: false
